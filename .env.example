############################
# Odds Intelligence (local)
############################

# SQLite DB path the API reads from.
# If unset, the API auto-picks:
# - data/demo_odds.db if present
# - otherwise data/odds.db
ODDS_DB_PATH=

# Create helpful indexes on startup (idempotent).
# If your DB file is on a read-only volume, set to 0.
ODDS_ENABLE_INDEXES=1

# "Online" threshold for book status cards.
ODDS_BOOK_ONLINE_WITHIN_MINUTES=90

# Optional: exclude a book from monitoring calculations/UI.
# Example: ODDS_EXCLUDED_BOOKS=synot
ODDS_EXCLUDED_BOOKS=

# API
API_PORT=8000

# Web (served by nginx in Docker)
WEB_PORT=3000

